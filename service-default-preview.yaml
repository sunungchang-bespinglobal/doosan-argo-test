apiVersion: v1
kind: Service
metadata:
  name: doosan-default-service-preview
spec:
  selector:
    saas/tenant.name: doosan
    app: doosan-default-service-preview
    version: v1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP