apiVersion: v1
kind: Service
metadata:
  name: doosan-default-service-preview
spec:
  selector:
    app: doosan-default
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP