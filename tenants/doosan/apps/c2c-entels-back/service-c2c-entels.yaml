apiVersion: v1
kind: Service
metadata:
  name: doosan-c2c-entels-service
spec:
  selector:
    app: doosan-c2c-entels-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP