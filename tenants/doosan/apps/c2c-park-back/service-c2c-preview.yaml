apiVersion: v1
kind: Service
metadata:
  name: doosan-c2c-park-service-preview
spec:
  selector:
    app: doosan-c2c-park
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP